apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: frontend-appset-rajaatt62431017
spec:
  generators:
    - git:
        repoURL: https://github.com/cassiecsouza/workshop-rajaatt62431017.git
        revision: main
        files:
        - path: "gitops-workshop/cluster-config/**/config.json"
  template:
    metadata:
      name: '{{.environment}}-{{.username}}-frontend'
      labels:
        harness.io/envRef: '{{.environment}}'
        harness.io/serviceRef: '{{.harness_service}}'
        harness.io/buildRef: '{{.image_tag}}'
    spec:
      project: 51687079
      source:
        repoURL: https://github.com/cassiecsouza/workshop-rajaatt62431017.git
        targetRevision: main
        path: "gitops-workshop/kustomize/frontend"
        kustomize:
          images:
          - cassiesouza/workshop:{{.image_tag}}
      destination:
        server: '{{.server_address}}'
        namespace: '{{.environment}}-{{.username}}-frontend'
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - PruneLast=true
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
